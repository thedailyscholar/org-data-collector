
"""
settings.py
Central place to load environment variables from `.env`.

This keeps secrets (API keys, tokens) out of the source code.
- Configure database settings here(Host,Port, Name, User, Password)
- Load API keys and tokens from .env here
"""